#include <stdio.h>
#include <stdlib.h>

//////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////
int main(void)
{
    char ch=0;
    int count=0;
    while((ch=getchar()) !='\n')
        if(ch==' ')
            count++;
    printf("%d",count+1);
    return 0;
}